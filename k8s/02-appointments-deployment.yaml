apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointments-service
  namespace: automex
  labels:
    app: appointments-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: appointments-service
  template:
    metadata:
      labels:
        app: appointments-service
    spec:
      containers:
      - name: appointments-service
        image: automex/appointments-service:1.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5000
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: appointments-service
  namespace: automex
  labels:
    app: appointments-service
spec:
  type: ClusterIP
  selector:
    app: appointments-service
  ports:
  - port: 5000
    targetPort: 5000
    name: http
